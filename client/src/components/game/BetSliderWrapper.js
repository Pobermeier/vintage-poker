import styled from 'styled-components';

export const BetSliderWrapper = styled.div({
  gridColumn: '1 / 5',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: '#282215',
  opacity: '0.85',
  borderRadius: '2rem',
  padding: '0.5rem 1rem',
});
